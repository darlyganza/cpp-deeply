#include <iostream> //including the header files for the processor directives
using namespace std; //declaration of variables, and functions as globally available
int main(){
	cout <<"Hello world"; //cout that of the iostream that embedded with stream insertion operators
	return 0; // the return value to the main function
}
