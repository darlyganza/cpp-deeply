//program implementing cpp functions: independent but smaller components in cpp programs:
#include <iostream>
#include <Math.h>
using namespace std;
int main(){
	int myNum = 16;
	cout<<sqrt(myNum);
	return 0;
}
